@model MvcCV.Models.Entity.TblCertificiates
@{
    ViewBag.Title = "NewCerti";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h2>New Certificate</h2>


<style>
    body {
        background-color: #B0A8B9; /* Daha koyu gri arka plan rengi */
        color: #ffffff;
        font-family: Arial, sans-serif;
    }

    .container {
        background-color: #ffffff; /* Beyaz arka plan rengi */
        padding: 40px;
        border-radius: 12px;
        box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
        max-width: 600px;
        margin: auto;
        margin-top: 50px;
    }

    .form-container {
        background-color: #f8f9fa; /* Açık gri arka plan rengi */
        padding: 20px;
        border-radius: 8px;
    }

    .form-label {
        font-weight: bold;
        margin-top: 15px;
        color: #343a40;
    }

    .form-control {
        padding: 10px;
        border: 1px solid #ced4da;
        border-radius: 5px;
    }

    .btn-update {
        font-size: 1.2em;
        padding: 12px 25px;
        margin-top: 20px;
        background-color: #28a745;
        border: none;
        border-radius: 5px;
        transition: background-color 0.3s;
    }

        .btn-update:hover {
            background-color: #218838;
        }

    .header {
        margin-bottom: 40px;
    }

        .header h2 {
            font-size: 2em;
            margin-bottom: 10px;
        }

        .header p {
            font-size: 1.2em;
            color: #6c757d;
        }
</style>

<div class="container">
    <div class="header text-center">
        <h2>Certificates Add Page</h2>
        <p>Fill in the form below to add Certificates to your profile.</p>
    </div>

    <div class="form-container">
        <form method="post">
            <div class="form-group">
                @Html.Label("Organizator :", new { @class = "form-label" })
                @Html.TextBoxFor(x => x.organizator, new { @class = "form-control", placeholder = "Enter Organizator Company" })
            </div>
            <div class="form-group">
                @Html.Label(" Commnet :", new { @class = "form-label" })
                @Html.TextBoxFor(x => x.comment, new { @class = "form-control", placeholder = "Enter experience ratio" })
            </div>
            <div class="form-group">
                @Html.Label("Date :", new { @class = "form-label" })
                @Html.TextBoxFor(x => x.date, new { @class = "form-control", placeholder = "Enter the Date" })
            </div>
            <div class="text-center">
                <button type="submit" class="btn btn-success btn-update">Update</button>
            </div>
        </form>
    </div>
</div>

